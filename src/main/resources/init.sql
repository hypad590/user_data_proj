create table userData (
                          id          BIGSERIAL PRIMARY KEY ,
                          name        TEXT      NOT NULL ,
                          password    TEXT      NOT NULL,
                          gmail       TEXT      NOT NULL
);